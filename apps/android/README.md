# Legacy Android Shell

Deprecated. VibeDeck now uses the Host-served browser/PWA path on Android, iPhone, and BOOX. The Host no longer serves APKs, native deep links, native QR codes, or the native H.264 WebSocket endpoint.

The source under this directory is retained for historical reference only and is not part of the supported build or release flow.
